# Autogenerated by https://github.com/magentaaps/meta

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

defaults:
  run:
    working-directory: v2

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
      - uses: actions/setup-go@v2.1.5
        with:
          go-version: 1.17
      - run: go version
      - run: go build .
      - run: go vet .
